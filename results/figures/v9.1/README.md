# v9.1 —— 水约束换为官方用水总量控制指标口径

分支 `feature/industrial-sectors`，起始提交 `a2afaf7`。

**v9.1 与 v9 的图不得相减、不得拼进同一张图。**（CLAUDE.md §二.6）
两者的输入版本相同（89 汇 / 2 651 边 / 666 节点），但**约束结构不同**——这比换输入更严重：
v9 的水规则是一条作用在耗水上的节点约束，v9.1 是两条作用在两个口径上的约束（见 §二）。
任何"v9.1 − v9"的差值都混着口径变更，不是任何一个物理量。

---

## 一、这批图基于什么

| 项 | 值 |
|---|---|
| 输入版本 | 与 v9 相同：89 汇 / 2 651 候选边 / 666 节点，绕行系数 1.136 |
| 新增输入 | `inputs/water_basin_caps.csv`（36 行 = 9 流域 × 4 规划年）；`plants.csv` 增 4 列（直流/空冷取水强度，逐机组精确，非重建） |
| 水预算 | `water_budget = "official_quota"`（`OptimizationAssumptions`） |
| 情景族 | `_oq` 后缀，见 `scripts/run_single.py` |
| 线程数 | 8（CLAUDE.md §二.1） |
| MIPGap | `_oq` 族全部 1%，**seed 族不得放宽** |
| 情景闭包 | **20 个**（BASE + 19 个 `_oq*`），即主图与附录图所需的最小集合 |
| 已求解情景 | 见 §三（2026-09-08 起跑，2 路 × 8 线程，约 3.5 h） |

---

## 二、和 v9 到底哪里不一样

v9（`_wd085` 族）：

```
节点可用量 = 订正径流 × WATER_EXTRACTABLE_FRACTION(0.20) × (1 − existing_withdrawal_share(0.85))
约束作用在：耗水
```

`optimization/data_prep.py` 自己的注释写着这两个系数是**别名化的**——求解器只看得见乘积
0.03，所以任何靠动 `existing_withdrawal_share` 做的对照，都分不清"生态流量标准在起作用"
还是"分配规则在起作用"。而且分母是当地天然径流，对北方流域根本不成立：海河的实际用水就
超过它自己的径流，靠南水北调和地下水撑着。

v9.1（`_oq` 族）把这一条拆成两条，各自约束它本来就是在讲的那个量：

```
节点   ≤ 订正径流 × 0.20                          生态流量规则，作用在耗水（枯竭规则）
流域   ≤ 用水总量控制指标 − 非电既有取水            分配规则，  作用在取水（公报计量口径）
```

`existing_withdrawal_share` 在 `_oq` 族里不再使用——分配规则改成从国办发〔2013〕2号读，
不再假设。**两者不能同时设**，否则分配规则被算两遍。

### 口径是这次最容易错的地方

| | 直流冷却贯流水量 | 全国煤电（本模型） |
|---|---|---|
| 取水定额（水节约〔2019〕373号） | **不计** | 96.2 亿 m³ |
| 《公报》用水量 ← 指标对它执法 | **计入**（直流火核电 453.8 亿是工业用水子项） | 521.1 亿 m³（标定后） |
| 耗水量 | 不适用 | 60.6 亿 m³ |

指标是对**用水量**执法的，所以流域约束必须作用在**取水**。用定额会低估约 5 倍。

模型原始取水强度是 Macknick(2011) 美国值，算出煤电一家 984 亿 m³/yr，比全国工业用水总量
953.7 还大；按 `basin_bias_factors` 同一乘性做法标定 k=0.495 → 521.1 亿。
自洽性检验：西北诸河标定前后同为 11.2 亿（该流域无直流机组），与公报该栏 0.0 一致。

**副作用（很重要）**：在取水口径下，空冷改造消掉的是整个贯流量约 90 m³/MWh，
而不是耗水口径下的约 1 m³/MWh。所以空冷从"省一点水"变成对指标最有效的合规手段，
`WA_cwatm_126_dry_oq_noair` 就是测这个的。

---

## 三、求解与出图状态

### 三之一、管线状态

| 环节 | 状态 |
|---|---|
| 官方指标数据（31 省、公报表9） | ✅ 已核对，零残差 |
| 省→流域分摊 | ✅ `builders/water_quota.province_basin_shares` |
| `inputs/water_basin_caps.csv` | ✅ 已生成（9 流域 × 4 规划年） |
| 取水强度标定 + 逐路径矩阵 | ✅ `_withdrawal_matrices`，k = 0.4949 |
| 流域上限约束 | ✅ `solver.py` `water_basin_limit_*` |
| 情景注册 | ✅ `run_single.py` 20 个（含 `_oq_capfree`，fig5 单因子对照，2026-09-08 补） |
| 输出可见性 | ✅ `slack_detail` / `resource_use` / `sanity_checks` 三表均带流域指标行 |
| 正式求解 | 🔄 进行中，`_v91tree/campaign.log` |
| 出图 | 🔄 见 §三之三 |

### 三之二、约束行为（真实 1% gap 求解，可引用）

`WA_cwatm_126_dry_oq`，8 线程，MIPGap 0.96%，status optimal。

**流域指标利用率**（`resource_use.csv`，`resource_type = water_basin_quota`）：

| 流域 | 2030 | 2040 | 2050 | 2060 |
|---|---|---|---|---|
| K 西北诸河 | **1.000** | **1.000** | **1.000** | **1.000** |
| F 长江 | 0.493 | 0.564 | 0.338 | 0.338 |
| H 珠江 | 0.425 | 0.480 | 0.474 | 0.474 |
| C 海河 | 0.287 | 0.362 | 0.246 | 0.253 |
| E 淮河 | 0.229 | 0.281 | 0.167 | 0.168 |
| D 黄河 | 0.135 | 0.152 | 0.108 | 0.078 |
| A / G / J | ≤ 0.17 | | | |

`sanity_checks.csv` 的 `water_basin_quota_breach` 四年全部 `pass`、值 `0.0`，
`slack_detail.csv` 零行。**即：K 流域四个规划年恰好顶在指标上，且没有付任何 big-M 松弛
——约束是靠改造满足的，不是靠买缺口。**

### 三之三、三级阶梯的实际响应（2030 年，全国）

| 量 | BASE | 仅生态流量 | ＋总量指标 | 第一步 | 第二步 |
|---|---|---|---|---|---|
| 空冷改造容量（GW） | 44.4 | 92.5 | 134.8 | **+48.1** | **+42.3** |
| 耗水量（亿 m³） | 57.1 | 53.5 | 50.6 | −3.6 | −2.9 |
| 生物质容量（GW） | 2.5 | 5.5 | 7.9 | +3.0 | +2.4 |
| 捕集量（Mt） | 0 | 0 | 0 | 0 | 0 |
| 退役容量（GW） | 0 | 0 | 0 | 0 | 0 |

目标函数：13.96604e12 → 13.96931e12 → 14.03227e12（+0.023%，+0.451%）。

**两条规则的物理响应都很大，而且量级相当**，尽管第一步的成本差只有 0.023%——落在 MIP gap
之内。**成本差与物理响应是两个不同的测量，不得用前者判断后者是否发生。**

逐流域（2030 年空冷改造容量增量，GW）：

| 流域 | BASE | 仅生态流量 | ＋总量指标 | 第一步 | 第二步 |
|---|---|---|---|---|---|
| D 黄河 | 12.1 | 34.7 | 28.5 | **+22.6** | −6.2 |
| K 西北内陆河 | 24.4 | 50.0 | 99.1 | **+25.5** | **+49.2** |
| C 海河 | 3.7 | 3.7 | 3.0 | 0.0 | −0.7 |
| A / E / F / G / H | — | — | — | 0.0 | 0.0 |

生态流量档的响应全部落在黄河与西北内陆河；总量指标档几乎全部落在西北内陆河，
黄河反而把一部分改造让回去。**两档的作用点不同，这正是拆开它们的意义。**

### 三之四、逐图状态（`_v91tree/render_probe.sh` 实测，9/19 情景时）

| 图 | 脚本 | 状态 | 备注 |
|---|---|---|---|
| Fig 1 水足迹 | `plot_fig1_water_footprint` | ✅ | 不依赖求解；面板 c 的流域压力已改为两档并列 |
| Fig 2 约束响应 | `plot_fig2_constraint_response` | ✅ | b、c 已重做；c 的地板待 seed 族 |
| Fig 3 机理 | `plot_fig3_mechanism` | ✅ | 阶梯标签改为两档；面板 a、c 的标题改为计算得出 |
| Fig 4 网络重构 | `plot_fig4_network_reconfiguration` | ⏳ | 待 seed 族 |
| Fig 5 路径演替 | `plot_fig5_pathway_succession` | ⏳ | 待 `_oq_envonly_nobias` 两个与 `_oq_capfree` |
| ED 流域特写 | `plot_ed_basin_closeup` | ✅ | **新图**，取代 `plot_ed_hai_closeup`（已删除） |
| ED 水量口径 | `plot_ed_water_basis` | ✅ | 分组标签由"超出配额"改为"北方缺水流域" |
| ED 谁在改造 | `plot_ed_who_converts` | ✅ | 两个标题改为计算得出（38%→75%，海河→黄河/西北） |
| ED 机队总览 / 汇网络 / 多跳匹配 | `plot_ed_fleet_atlas` / `plot_ed_sink_network` / `plot_ed_multihop_matching` | ✅ | |
| ED 生物质来源 / 回退 / 分省转型 / 方差分解 / 敏感性 | 同名脚本 | ✅ | |
| ED 水开关 | `plot_ed_water_on_off` | ⏳ | 待 seed 族 |
| ED 减排—水 | `plot_ed_water_abatement` | ⏳ | 待 seed 族 |
| ED 源汇匹配 | `plot_ed_source_sink_matching` | ⏳ | 待 seed 族 |
| Fig 3 归因 / ED 源汇分年 | `plot_fig3_attribution` / `plot_ed_source_sink_matching_years` | ⛔ | `render_version.py` 的 SKIP 名单，v9 起即不在图序列内 |

> `plot_ed_sensitivity` 需要 22 个 `SA_*` 情景，v9 也从未求解过，因此**不是回归**；
> 它当前能出图，但画的是空的敏感性轴。

### 三之五、三级阶梯（2040 年空冷与退役、2060 年捕集，三臂中位数）

| 量 | BASE | 仅生态流量 | ＋总量指标 | 第一步 | 第二步 |
|---|---|---|---|---|---|
| 空冷改造容量（GW，2040） | 30.5 | 67.7 | 140.4 | **+37.2** | **+72.7** |
| CO₂ 捕集量（Mt，2060） | 797.0 | 793.1 | 787.4 | −3.9 | −5.7 |
| 提前退役容量（GW，2040） | 2.8 | 2.9 | 3.0 | +0.1 | +0.1 |

简并度地板待 seed 族求解后回填。**在地板测出来之前，上表任何一步都不得称为"效应"**——
按 CLAUDE.md §二.4 的既有结论，本模型上只有目标函数与空冷转换量越过过各自的地板。

## 四、相对 v9 哪些数字变了

尚无求解结果，只有预算侧的变化（`scripts/diagnose_official_water_budget.py` 可复跑）。
指标年 2030，单位 亿 m³/yr：

| 流域 | v9 可用量(0.03×径流, 耗水口径) | v9.1 流域余量(取水口径) | v9.1 煤电取水 | 占余量 |
|---|---|---|---|---|
| A 东北诸河 | — | 239 | 23.4 | 10% |
| C 海河 | — | 139 | 40.1 | 29% |
| D 黄河 | — | 114 | 18.1 | 16% |
| E 淮河 | — | 236 | 54.2 | 23% |
| F 长江 | — | 595 | 292.5 | **49%** |
| G 东南诸河 | — | 148 | 14.6 | 10% |
| H 珠江 | — | 159 | 66.8 | 42% |
| J 西南诸河 | — | 38 | 0.0 | 0% |
| K 西北诸河 | — | **2.1**（原始 −85.5） | 11.2 | **须减 81%** |
| 全国 | — | 1 582 | 521.1 | 33% |

（v9 列留空是有意的：两列不同口径，并排列出会诱导相减。v9 的可用量见 v9/README。
上表的流域余量已扣除已建模工业点源的现状取水 73.9 亿，见 §五。）

### 四之二、约束在哪里咬住——不需要求解就能定的边界

把全机队分别按四种极端配置定价，看哪些流域的指标被突破（亿 m³/yr）：

| 流域 | 余量 | 全部未改造 | 全部 CCS | 全部空冷 | 空冷+CCS |
|---|---|---|---|---|---|
| A 东北诸河 | 239.0 | 23.4 | 42.4 | 0.8 | 1.6 |
| C 海河 | 139.1 | 40.1 | 72.4 | 1.6 | 3.2 |
| D 黄河 | 114.0 | 18.1 | 34.8 | 2.5 | 5.0 |
| E 淮河 | 236.3 | 54.2 | 98.0 | 1.5 | 3.2 |
| F 长江 | 594.8 | 292.5 | **511.6** | 2.7 | 5.6 |
| G 东南诸河 | 148.5 | 14.6 | 25.8 | 0.8 | 1.7 |
| H 珠江 | 159.4 | 66.8 | 118.2 | 1.4 | 2.9 |
| J 西南诸河 | 37.8 | 0.0 | 0.0 | 0.0 | 0.0 |
| K 西北诸河 | **2.1** | **11.2** | **22.6** | 1.3 | 2.6 |
| 全国 | 1 582 | 521.1 | 925.9 | 12.5 | 25.8 |

**结论（预算侧，先于求解成立）：流域指标只在西北诸河一个流域咬得住，而且那里靠空冷改造
就能解开（11.2 → 1.3，低于 2.1 的余量）。** 连最耗水的配置——全机队上 CCS，全国 925.9
亿 m³——在其余八个流域都装得下；最紧的长江是 511.6 / 594.8 = 86%，仍有余。

> **⚠️ 这一段的后半句原来写错了，2026-09-08 据真实求解更正。**
>
> 原文写"生态流量那一半也不咬：节点预算合计 1 535 亿 m³（耗水口径），机队耗水 60.6 亿，
> 占 3.9%"。**这是拿流域合计去判断一条逐节点约束是否松弛，正是本仓库反复警告的那个错误**
> （CLAUDE.md、fig3(c) 的存在理由）。真实求解表明生态流量档**咬得很紧**：仅它一条就把
> 2030 年全国空冷改造从 44.4 GW 推到 92.5 GW（+48.1 GW），与总量指标档再加的 +42.3 GW
> 量级相当（§三之三）。合计占用 3.9% 与"约束不起作用"是两回事——流域内部分节点已经贴到
> 各自上限，而合计看起来很宽。

所以准确的说法是：**两条规则都咬得住，但咬的方式和地点不同。** 生态流量规则在节点层面咬，
落点是黄河与西北内陆河；总量指标规则在流域层面咬，落点是西北内陆河一个流域。
原口径 `_wd085` 的问题不是"太紧"，而是把这两件事乘成了一个没有制度对应物的 0.03 系数，
以至于任何结果都说不清是哪一条在起作用。

这不是坏消息，而是把问题换成了一个更有意思的：**煤电一家装得下，煤电 + 工业脱碳
一起还装得下吗？** 两者争的是同一套流域指标，而绿氢路线的电解水还要额外耗水。
长江已经用掉 86%，工业正在同一个流域里。这是 v9.1 该讲的故事，不是煤电单独撞墙。

---

## 五、哪些结论不许从这批图里读

1. **不许把 v9.1 的任何量与 v9 相减。** 口径不同，差值无物理意义。
2. **不许把预算侧的"装得下"读成"约束不起作用"。** 全机队全 CCS 的取水在八个流域装得下
   （最紧的长江 86%），但这**不等于**约束松弛：真实求解里生态流量档与总量指标档各自推动了
   +48.1 与 +42.3 GW 的空冷改造（§三之三）。逐节点约束不能用流域合计判断。
   同样，长江在全 CCS 下已用掉 86% 的余量，而工业点源在同一流域里争同一套指标——
   煤电单独的结论不能外推到煤电 + 工业。
3. **西北诸河超指标这一判断对分摊权重敏感。** 需水权重下压缩前为 −85.5 亿，面积权重下会翻成正。
   必须同时报两个权重的结果，不得只报一个。超指标流域的预留按 `compliance_ratio`
   等比压缩（全用户同比例压减，不是只压电力），压缩后煤电在西北诸河只剩 2.1 亿 m³ 合法空间。
4. **未核实假设：`用水总量` 考核是否把直流冷却水全额计入。** 本版按《公报》的表述假设
   "计入"。**但这条不确定性是单向的**：若实际考核对直流回归水另有处理，机队对指标的占用
   只会更小（长江从 49% 掉到约 3%），约束更松。所以它**动摇不了**§四之二 的结论，
   只会加强它。仍应在方法学里写明按哪一条口径执行，但不再是投稿前的阻断项。
5. **工业部门的水已从预留里扣出（73.9 亿 m³/yr），但工业还没进求解器。**
   在工业成为决策变量之前，这部分余量是空着的，不得解读为"工业有多少水可用"。
6. **`existing_withdrawal_share = 0.85` 的论证链在本版作废**，相关的
   `_wd085` 敏感性轴（wd056/wd070/wd090）在 `_oq` 下不存在。图 2 的面板 b、c 已重做：
   b 改为逐流域两档利用率（对数轴，各按自己的分母），c 改为 BASE→仅生态流量→＋总量指标
   的三级阶梯响应分解，每一步配简并度地板，未越过地板者标"未分辨"。
7. **两档的"最紧流域"不是同一个，也不要与"响应最大的流域"混为一谈。**
   按反事实紧度：生态流量档只有海河越 100%（全捕集 102.7%，现状 55.5%），
   总量指标档只有西北诸河越限（全捕集 1078%，现状已 533%）。
   按实际响应：生态流量档落在黄河与西北内陆河，海河一动不动——因为 2030 年排放目标尚不
   约束、各情景捕集量均为 0，海河那个 102.7% 是模型并不选择的全捕集反事实。
   ED 流域特写按**响应**选流域（黄河 | 西北内陆河），Fig 2(b) 报**紧度**排名，两者不得互换。
8. **配额切片必须点明。** 枯水期配额随 (年份, SSP 成员集) 变：海河在 2060/SSP3-7.0 下是
   16.84 亿 m³，在 2030/全 20 成员下是 15.76。Fig 1(c) 用前者，Fig 2(b) 与 ED 特写用后者，
   `plot_fig1_water_footprint.basin_drying(year, ssp)` 现在强制调用方指定。**跨图引用前先对切片。**
9. **厂址→流域有两套归属规则，合计量会差几个百分点。** ED 特写用"最近可达供水节点"
   （`ed_plant_data.plant_basin`），Fig 2/3 用"最近流域多边形"（`plot_style.assign_basin`），
   共同覆盖厂址上一致率 93.5%。黄河第一步：ED 图 20 GW，`hub_frame` 口径 22.6 GW。
   **两处的数不要互相引用。**

---

## 六、复现

```bash
python scripts/build_water_basin_caps.py          # 生成 inputs/water_basin_caps.csv
python scripts/diagnose_official_water_budget.py  # 只打印，核对预算
python scripts/run_single.py WA_cwatm_126_dry_oq  # 求解（8 线程，MIPGap 1%）
```

方法与来源：`docs/官方指标口径水预算.md`（本目录已复制一份）。
